// /home/bruno/ulbra/2019-1/ap2/vetor_string/vt_string.c
// Criado em:seg 25/mar/2019 hs 19:22
/* Propósito:Variável indexada string */
// Cadeira: APII
// Autor: Bruno César Angst; (bruno.angst@rede.ulbra.br)

#include <stdio.h>
#include <string.h>
int main()
{
	//char nome[tamanho];
	//char palavra0[15];
	//char palavra1[15] = "ULBRA";
	//char palavra2[] = "UNIVERSIDADE";
	//char palavra3[] = {'U','L','B','R','A'}
/* Leitura:
	scanf("%s", palavra1) - lê a string inteira
	scanf("%c", &palavra1[0]) - é necessário atribuir o
	terminador'\0' ao final.
	gets(palavra1)

	Saida:
	printf("Você digitou: %s\n", palavra1);
	printf("%c\n", palavra[0]);

	Atribuição:
	palavra1[1] = 'x';
	palavra1 = "ULBRA"; - NÃO FUNCIONA
	ENTÃO
	strcpy(string1, string2)
	equivale a string1 = string2
	assim
	strcpy(palavra1, "ULBRA")
	
	Comparação:
	if (palavra1 == palavra2) - não funciona!
	strcmp(string1, string2) - retorna 0 se conteúdos são iguais.
	if (strcmp(palavra1, palavra2) == 0) {
		printf("As strings são iguais!\n");
	}

	Concatenação:
	strcat(string1, string2) - string2 sera concatenado no final da
	string1.

	Verfica se um caracter aparece na estring
	strchr(string, caracter) - retorna verdadeiro se o caracter pertence a
	string.



	*/
	return 0;
}
