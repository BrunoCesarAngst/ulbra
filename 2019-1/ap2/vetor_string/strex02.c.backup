// /home/bruno/ulbra/2019-1/ap2/vetor_string/strex02.c
// Criado em:qua 27/mar/2019 hs 12:06
/* Propósito: Ler um valor n que representa o número de pares de palavras(2 palavras) que serão lidas.  seguir ler os n pares e imprimir para cada par:
	-IGUAIS se as palavras informadas (do par) forem iguais.
	-DIFERENTES se as palavras informadas(do par) forem diferentes.*/
// Cadeira: APII
// Autor: Bruno César Angst; (bruno.angst@rede.ulbra.br)

#include <stdio.h>
#include <string.h>

int main()
{
	int	num_pa, i;
	char palavra[15];

	printf("Número de palavras: \n");
	scanf("%i", &num_pa);


	for (int i = 0; i < num_pa; i++) {
		for (int i = 0; i < 2; i++) {

		printf("Digite uma palavra: \n");
		gets("%s", palavra);
			
		}
	}

	return 0;
}
