// /home/bruno/ulbra/2019-1/ap2/vetores/vtex10.c
// Criado em:seg 25/mar/2019 hs 16:17
/* Propósito: Escreva um programa que leia diversos números inteiros fornecidos pelo usuário e distribua esses valores entre dois vetores: o vetor dos ímpares e o vetor dos pares. Cada um terá tamanho 10. O término do algoritmo se dará quando o usuário digitar zero ou um dos vetores for totalmente preenchido. Mostrar os dois vetores no final.*/
// Cadeira: APII
// Autor: Bruno César Angst; (bruno.angst@rede.ulbra.br)

#include <stdio.h>

int main()
{
do {
	$
} while (num == 0 || );
	printf("Digite um número: ");
	scanf("%d", &num)
	return 0;
}
